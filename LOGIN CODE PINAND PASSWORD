#include <string>
#include <iostream>
int main() {
string password = Password;
    cout << "put password\n
    cout << "\npUT PASSWORD AGAIN\n";
    cin >> Password;
    cout << "CHECKING\n";
    if (contraseña == Contraseña) {
        cout << "CORRRRRECT\n";
    }
    else
    {
        cout << "Tru again\n";
        exit(1);
    }
    
    int pin;
    cout << "\nPlease set up a pin / digit code";
    cin >> pin;
    int Pin;
    cout << "SET THE SAME PIN / DIGIT CODE \n";
    cin >> Pin;
    if (pin == Pin) {
    cout << "CORRECT\n";
    }
    else {
    cout << "try again\n";
}
